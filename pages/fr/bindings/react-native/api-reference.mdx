---
title: Référence React Native API
description: Composantes publiques, registres, thèmes, contextes, typographie et actifs exportés par form0-react-native.
---

<PageBadges />

Toutes les exportations prises en charge sont disponibles à partir de `form0-react-native`.

## Rendu et thèmes

- `FormRenderer` — rend et gère une forme native pilotée par schema.
- `FormHeader` — l'en-tête de formulaire natif de binding.
- `ThemeProvider`, `ThemeContext`, `useTheme`
- `lightTheme`, `darkTheme`, `mergeThemes`, `getThemeByName`

Voir [FormRenderer](/fr/bindings/react-native/form-renderer) et
[Thèmes, polices et images](/fr/bindings/react-native/themes-assets).

## Registre sur le terrain

- `FieldRegistryProvider`, `FieldRegistryContext`, `useFieldRegistry`
- `createFieldRegistry`, `defaultFieldRegistry`
- `registerFieldComponent`, `unregisterFieldComponent`, `resetFieldComponents`
- `getFieldComponent`, `listRegisteredFieldTypes`, `getMissingFieldComponentTypes`
- `KNOWN_FIELD_TYPES`

Voir [Champ personnalisé renderers](/fr/bindings/react-native/custom-renderers).

## Images et typographie

- `ImageResolverProvider`, `ImageResolverContext`, `useImageResolver`
- `Text`, `TextInput`
- `form0Fonts` et les constantes `FORM0_FONT_FAMILY*`

Le paquet comprend des déclarations TypeScript pour les accessoires `FormRenderer`, snapshots, submission et submission.
enregistrer les métadonnées, les options du moteur, les valeurs de registre et les accessoires personnalisés du champ renderer.
composants individuels par défaut, les contrôleurs reproductibles et les utilitaires submission ne sont pas
exportations publiques.
