---
title: Importar y exportar CSV
description: Convierte definiciones de esquema entre JSON y la representación CSV de la CLI.
---

Usa `form0 schema export` para crear un CSV desde un esquema JSON y `form0 schema import` para
convertirlo de nuevo a JSON. Revisa y valida siempre el esquema generado.
