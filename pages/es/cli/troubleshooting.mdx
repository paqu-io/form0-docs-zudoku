---
title: Solución de problemas
description: Resuelve problemas habituales de instalación, proyecto, previsualización y conectores.
---

<PageBadges />

Comprueba la versión con `form0 --version` y consulta `--help`. Ejecuta los comandos desde el
directorio que contiene `package.json` y `form0.config.js`, y valida el esquema antes de iniciar la
previsualización.
