---
title: Risoluzione dei problemi
description: Risolvi i problemi comuni di installazione, progetto, anteprima e connettori.
---

<PageBadges />

Controlla la versione con `form0 --version` e consulta `--help`. Esegui i comandi dalla directory che
contiene `package.json` e `form0.config.js`, quindi valida lo schema prima di avviare l’anteprima.
