---
title: Validare e visualizzare l’anteprima
description: Controlla uno schema e ispeziona la sua struttura dalla riga di comando.
---

Usa `form0 validate <schema>` per validare uno schema e `form0 preview <schema>` per mostrarne la
struttura. Nella shell interattiva, carica lo schema e usa `validate` o `preview`.
